.DS_Store
.claude/*.local.*
.serena/
.liggare/
node_modules/
coverage/
RETRO-*.md
SWARM-*.md
PLAN-*.md
BRANCH-REVIEW-*
SYNERGY-*.local.md
PRIVATE-SYNERGY-*.md
evaluations/

# Beads / Dolt files — entire .beads/ directory is local-only
.beads/
.dolt/
*.db
.beads-credential-key

# Beads / Dolt files (added by bd init)
.beads/proxieddb/
.research/

# Generated by .claude/workflows/stale-npm-triage.js — a report, not source
stale-npm-triage.md

# Frozen raw inputs from the 2026-08-05 stale-npm-triage run, kept so `rank.mjs`
# can be re-run offline against a fixed scan/registry pair — that is what makes a
# ranking change attributable without a second 21-minute sweep. Working data with
# a known expiry, not a record; delete once the ordering rewrite is validated.
.baseline-2026-08-05/
.baseline-2026-08-06/
