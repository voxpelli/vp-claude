{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run check:plugin && npm run check:md && npm run check:sh && npm run check:hooks",
    "check:hooks": "node scripts/check-hooks.mjs",
    "check:plugin": "node validate-plugin.mjs",
    "check:md": "remark . --quiet --frail",
    "check:sh": "shellcheck hooks/*.sh scripts/*.sh && shfmt -d hooks/*.sh scripts/*.sh"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "remark-cli": "^12.0.0",
    "remark-frontmatter": "^5.0.0",
    "remark-preset-lint-consistent": "^6.0.0",
    "remark-preset-lint-recommended": "^7.0.0"
  },
  "remarkConfig": {
    "plugins": [
      "remark-frontmatter",
      "remark-preset-lint-consistent",
      "remark-preset-lint-recommended"
    ]
  }
}
