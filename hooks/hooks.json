{
  "description": "Basic Memory quality hooks: schema validation after writes, auto-reflect before compaction, graph summary at session start.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "mcp__basic-memory__write_note|mcp__basic-memory__edit_note",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "A Basic Memory note was just written or edited. Check if the note follows the expected structure for its type:\n\n1. **Package note types** — verify the note has: `packages` array in frontmatter, `## Observations` section with `[category]` tagged items, `## Relations` section with at least one `[[wiki-link]]`, and title matching the prefix convention for its type:\n   - `type: npm_package` → title must match `npm:<package-name>`\n   - `type: crate_package` → title must match `crate:<name>`\n   - `type: go_module` → title must match `go:<module/path>`\n   - `type: composer_package` → title must match `composer:<vendor>/<package>`\n   - `type: pypi_package` → title must match `pypi:<name>`\n   - `type: ruby_gem` → title must match `gem:<name>`\n\n2. **Tool note types** — verify the note has: `packages` array in frontmatter, `## Observations` section with `[category]` tagged items, `## Relations` section with at least one `[[wiki-link]]`, and title matching the prefix convention for its type:\n   - `type: brew_formula` → title must match `brew:<name>`\n   - `type: brew_cask` → title must match `cask:<name>`\n   - `type: github_action` → title must match `action:<owner>/<repo>`\n   - `type: docker_image` → title must match `docker:<image>`\n   - `type: vscode_extension` → title must match `vscode:<publisher>.<extension>`\n\n3. **All note types** — verify: valid YAML frontmatter with `title`, at least one `## Observations` or `## Relations` section.\n\nIf issues are found, provide a brief systemMessage describing what's missing. If the note looks well-formed, return with no systemMessage.\n\nDo NOT block the operation — this is informational feedback only.",
            "timeout": 15
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Context is about to be compacted. Before losing conversation history, review the current conversation for insights worth preserving in Basic Memory:\n\n1. Were any technical decisions made? (architecture choices, library selections, API designs)\n2. Were any debugging insights discovered? (root causes, non-obvious fixes, gotchas)\n3. Were any patterns or conventions established?\n4. Were any packages researched or evaluated?\n\nFor each valuable insight found, use `mcp__basic-memory__edit_note` to append observations to the most relevant existing note, or `mcp__basic-memory__write_note` to create a new note if no relevant note exists.\n\nKeep notes concise — capture the insight, not the full conversation. Use `[decision]`, `[lesson]`, `[gotcha]`, or `[pattern]` observation categories.\n\nIf nothing worth preserving was discussed, do nothing.",
            "timeout": 30
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
